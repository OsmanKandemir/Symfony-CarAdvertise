{% extends 'base.html.twig' %}

{% block title %}AdminImage{% endblock %}

{% block body %}
    <h1>AdminImage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admin_image.id }}</td>
            </tr>
            <tr>
                <th>Car_id</th>
                <td>{{ admin_image.carId }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ admin_image.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_image_index1') }}">back to list</a>



    {{ include('user_image/_delete_form.html.twig') }}
{% endblock %}
